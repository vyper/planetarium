Planetarium is feed aggregator writed in Ruby.

For install:
$ git clone http://github.com/vyper/planetarium.git

Open 'config/config.yml' and configure.

Run:
$ ./bin/planetarium update

For help or more information:
$ ./bin/planetarium help
